# Copiá este archivo como ".env" y completá el valor.
# El archivo ".env" está ignorado por git: nunca se sube al repositorio.

# API key de Google Gemini — se obtiene gratis en https://aistudio.google.com/apikey
GEMINI_API_KEY=
